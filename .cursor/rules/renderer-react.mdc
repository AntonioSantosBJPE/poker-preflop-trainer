---
description: React renderer — UI, Router, estado (spec §7).
globs: src/renderer/**/*.tsx,src/renderer/**/*.ts
alwaysApply: false
---

# Renderer

- React 18 + TypeScript + Tailwind; rotas conforme §7.1.
- Estado global com Zustand onde necessário; sem acesso a Node/SQLite no renderer.
- Consumir dados apenas via `window.api` (preload).
- Grid 13×13: convenções de `row_index`/`col_index` e suited/offsuit conforme skill `preflop-domain` (secção Grid 13×13).
