# TASK-01-shared-poker

## Objetivo

Lógica pura em `src/shared/poker`: mapeamento mão ↔ célula, contagem de combos, **avaliação** §3.4.3; testes Vitest.

## Pode editar

- `src/shared/poker/**`, `src/shared/constants.ts` (se necessário para ranks), testes `*.test.ts`.

## Não deve editar

- `src/main/ipc/**`, `src/renderer/**`.

## Critérios de pronto

- `pnpm test` passa; funções documentadas implicitamente pelo contrato em `CONTRACTS.md`.

## Contratos

- [docs/agents/CONTRACTS.md](../docs/agents/CONTRACTS.md)
