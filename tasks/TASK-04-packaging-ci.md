# TASK-04-packaging-ci

## Objetivo

`electron-builder` (Linux/Windows), scripts de build, CI com testes + typecheck + `electron-vite build`.

## Pode editar

- `electron-builder.yml`, `.github/workflows/*`, `package.json` scripts.

## Critérios de pronto

- Workflow CI verde no repositório remoto; artefactos ou pelo menos compile sem erro headless.

## Contratos

- N/A (infra).
