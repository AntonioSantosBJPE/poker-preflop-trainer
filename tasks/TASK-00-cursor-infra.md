# TASK-00-cursor-infra

## Objetivo

Criar governança para agentes: `.cursor/rules/`, `.cursor/skills/`, `docs/agents/*`, `tasks/*.md`, `AGENTS.md`.

## Pode editar

- `.cursor/rules/*.mdc`, `.cursor/skills/*/SKILL.md`, `docs/agents/CONTRACTS.md`, `docs/agents/TASKS_INDEX.md`, `tasks/TASK-*.md`, `AGENTS.md`.

## Não deve editar

- Código de produção em `src/**` salvo referências textuais necessárias.

## Critérios de pronto

- `TASKS_INDEX.md` lista ondas e liga cada task.
- `CONTRACTS.md` define enums, grid, IPC e ownership de merge.