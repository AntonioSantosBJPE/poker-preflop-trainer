# TASK-02-main-auth

## Objetivo

Implementar `auth:*` com bcrypt (12), JWT, keytar, tabela `users`, `auth:me`.

## Pode editar

- `src/main/ipc/auth.ts`, `src/main/services/session.ts`, testes associados se existirem.

## Não deve editar

- `situations.ts`, `training.ts`, `stats.ts`.

## Critérios de pronto

- Registo + login + logout + `me` funcionam com DB real.

## Contratos

- [docs/agents/CONTRACTS.md](../docs/agents/CONTRACTS.md)
